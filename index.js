
function vowelsOnly(str){
    for (i=0; i<(str).length(); i++){
       if (str.match(/[aeiouAEIOU]/gi)){
           return i;

        for (i= 0;i < str.length(); i++) {
        (str) += s.charAt(i);
    }
       }
       }
   }
